# Shared
BOT_TOKEN=123456:ABC-DEF...
WEBHOOK_BASE_URL=https://your-domain.example.com
WEBAPP_URL=https://your-domain.example.com

# Node server
SERVER_PORT=3000
NODE_ENV=development

# Python bot (webhook path is fixed: /webhook/bot on same host as WEBHOOK_BASE_URL)
# If bot runs on a different host, set BOT_WEBHOOK_PATH=/webhook/bot

# Vercel (Mini App + API):
# - Deploys website + /api/* functions
# - Needs BOT_TOKEN (for initData validation)
